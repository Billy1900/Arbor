apiVersion: v1
kind: Service
metadata:
  name: {{ include "arbor.fullname" . }}-api
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arbor.labels" . | nindent 4 }}
    {{- include "arbor.api.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arbor.api.selectorLabels" . | nindent 4 }}
